# Страница достижений

Страница отображает все достижения пользователя с информацией о разблокированных и заблокированных достижениях.

## Функциональность

### Отображение достижений

Страница разделена на две секции:

1. **Разблокированные достижения** - Достижения, которые пользователь уже получил
   - Отображаются с золотым фоном
   - Показывают дату разблокировки
   - Отображают награду в XP
   - Помечены галочкой ✓

2. **Заблокированные достижения** - Достижения, которые еще не получены
   - Отображаются с серым фоном
   - Иконка затемнена (grayscale)
   - Показывают прогресс к достижению
   - Помечены замком 🔒

### Статистика прогресса

В верхней части страницы отображается:
- Количество разблокированных достижений из общего числа
- Процент завершения
- Прогресс-бар

### Расчет прогресса

Для заблокированных достижений рассчитывается прогресс на основе критериев:

- **minStreak** - Прогресс по текущему стрику
- **minLevel** - Прогресс по текущему уровню
- **minXP** - Прогресс по накопленному XP
- **minContacts** - Прогресс по количеству контактов
- **minRemindersCompleted** - Прогресс по выполненным напоминаниям

Прогресс отображается в процентах и визуализируется прогресс-баром.

### Форматирование дат

Даты разблокировки отображаются в удобном формате:
- "Сегодня" - для сегодняшней даты
- "Вчера" - для вчерашней даты
- "X дн. назад" - для дат до недели
- "X нед. назад" - для дат до месяца
- "X мес. назад" - для более старых дат

## Компоненты

### Composable: useGamification

Страница использует composable `useGamification` для получения данных:

```typescript
const {
  achievements,           // Все достижения
  unlockedAchievements,  // Разблокированные достижения
  lockedAchievements,    // Заблокированные достижения
  achievementCount,      // Счетчик достижений
  fetchAchievements,     // Загрузить достижения
  fetchStats,            // Загрузить статистику
  stats,                 // Статистика пользователя
  error,                 // Ошибка
  isLoadingAchievements  // Состояние загрузки
} = useGamification()
```

### API Endpoints

Страница использует следующие API endpoints:

- `GET /api/gamification/achievements` - Получить все достижения
- `GET /api/gamification/stats` - Получить статистику пользователя

## Структура данных

### Achievement

```typescript
interface Achievement {
  id: number
  code: string
  name: string
  description: string
  icon: string
  xpReward: number
  criteria: Record<string, any>
  unlocked: boolean
  unlockedAt?: string
  progress?: number  // Добавляется на клиенте для заблокированных
}
```

### Criteria Types

```typescript
type Criteria = 
  | { minStreak: number }
  | { minLevel: number }
  | { minXP: number }
  | { minContacts: number }
  | { minRemindersCompleted: number }
```

## Состояния UI

### Загрузка

Отображается спиннер во время загрузки данных.

### Ошибка

Отображается сообщение об ошибке с кнопкой "Попробовать снова".

### Пустое состояние

Если нет достижений в системе, отображается сообщение "Нет достижений".

### Нормальное состояние

Отображаются все достижения, разделенные на разблокированные и заблокированные.

## Навигация

Страница доступна по маршруту `/achievements`.

Ссылка на страницу добавлена на главной странице (Dashboard) в секции достижений.

## Стилизация

Страница использует UnoCSS для стилизации:

- **Разблокированные достижения**: Градиент от yellow-50 до yellow-100, граница yellow-300
- **Заблокированные достижения**: Белый фон, граница gray-200, opacity-75
- **Прогресс-бары**: Градиент от purple-500 до purple-600 (общий прогресс), от blue-400 до blue-500 (прогресс достижений)

## Требования

Страница реализует следующие требования из спецификации:

- **Требование 4.10**: Отображение всех достижений с состояниями заблокировано/разблокировано
- **Требование 5.3**: Отображение прогресса к заблокированным достижениям

## Примеры использования

### Навигация на страницу

```vue
<NuxtLink to="/achievements">
  Посмотреть все достижения
</NuxtLink>
```

### Программная навигация

```typescript
const router = useRouter()
router.push('/achievements')
```

## Тестирование

Тесты для страницы находятся в `tests/unit/pages/achievements.test.ts`.

Тесты покрывают:
- Расчет процента завершения
- Расчет прогресса к достижениям
- Форматирование дат
- Структуру данных достижений
- Фильтрацию достижений

Запуск тестов:
```bash
npm test
```

## Будущие улучшения

- Добавить анимацию при разблокировке достижения
- Добавить звуковые эффекты
- Добавить возможность делиться достижениями
- Добавить детальную страницу для каждого достижения
- Добавить фильтрацию и сортировку достижений
- Добавить категории достижений
