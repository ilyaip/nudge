# 🏗️ Архитектура проекта Nudge

## 📊 Общая схема

```
┌─────────────────────────────────────────────────────────────┐
│                     Пользователь                            │
│                    (Telegram User)                          │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                  Telegram Client                            │
│              (Telegram Mini App)                            │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ initData (signed)
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                   Nuxt 3 Frontend                           │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Pages (File-based Routing)                          │  │
│  │  ├── index.vue (Dashboard)                           │  │
│  │  ├── contacts.vue (Contact List)                     │  │
│  │  └── contacts/[id].vue (Contact Details)            │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Composables (Business Logic)                        │  │
│  │  ├── useContacts.ts                                  │  │
│  │  ├── useReminders.ts                                 │  │
│  │  ├── useGamification.ts                              │  │
│  │  └── useTelegramAuth.ts                              │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Stores (Pinia - State Management)                   │  │
│  │  └── auth.ts                                         │  │
│  └──────────────────────────────────────────────────────┘  │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ API Calls
                         ▼
┌─────────────────────────────────────────────────────────────┐
│              Nuxt API Routes (Nitro Server)                 │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Authentication                                       │  │
│  │  └── POST /api/auth                                  │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Contacts Management                                  │  │
│  │  ├── GET    /api/contacts                            │  │
│  │  ├── POST   /api/contacts                            │  │
│  │  ├── GET    /api/contacts/:id                        │  │
│  │  ├── PUT    /api/contacts/:id                        │  │
│  │  └── DELETE /api/contacts/:id                        │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Reminders                                            │  │
│  │  ├── GET  /api/reminders                             │  │
│  │  └── POST /api/reminders/:id/complete                │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Gamification                                         │  │
│  │  ├── GET /api/gamification/stats                     │  │
│  │  └── GET /api/gamification/achievements              │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Webhook                                              │  │
│  │  └── POST /api/webhook                               │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Health Check                                         │  │
│  │  └── GET /api/health                                 │  │
│  └──────────────────────────────────────────────────────┘  │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ SQL Queries (Drizzle ORM)
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                    PostgreSQL Database                      │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Tables:                                              │  │
│  │  ├── users                                            │  │
│  │  ├── contacts                                         │  │
│  │  ├── reminders                                        │  │
│  │  ├── achievements                                     │  │
│  │  ├── user_achievements                                │  │
│  │  └── activity_logs                                    │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                         ▲
                         │
┌────────────────────────┴────────────────────────────────────┐
│                  Background Jobs                            │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Node-cron Scheduler                                  │  │
│  │  └── Daily reminder calculation                       │  │
│  └──────────────────────────────────────────────────────┘  │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ Send notifications
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                    Telegram Bot API                         │
│  └── Send messages to users                                 │
└─────────────────────────────────────────────────────────────┘
```

## 🐳 Docker Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    Docker Compose                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌────────────────────┐      ┌────────────────────┐       │
│  │   Nuxt App         │      │   PostgreSQL       │       │
│  │   Container        │◄─────┤   Container        │       │
│  │                    │      │                    │       │
│  │   Port: 3000       │      │   Port: 5432       │       │
│  │   Node 20 Alpine   │      │   Postgres 15      │       │
│  │                    │      │                    │       │
│  │   Health Check ✓   │      │   Health Check ✓   │       │
│  │   Auto Restart ✓   │      │   Auto Restart ✓   │       │
│  └────────────────────┘      └────────┬───────────┘       │
│           ▲                            │                    │
│           │                            │                    │
│           │                   ┌────────▼───────────┐       │
│           │                   │  Persistent Volume │       │
│           │                   │  (postgres_data)   │       │
│           │                   └────────────────────┘       │
│           │                                                 │
│  ┌────────┴───────────┐                                    │
│  │   Adminer          │                                    │
│  │   Container        │                                    │
│  │                    │                                    │
│  │   Port: 8080       │                                    │
│  │   DB Management UI │                                    │
│  └────────────────────┘                                    │
│                                                             │
│  Network: nudge-network (bridge)                           │
└─────────────────────────────────────────────────────────────┘
```

## 📁 Структура файлов

```
nudge-telegram-app/
├── 📄 Конфигурация
│   ├── package.json              # Зависимости и скрипты
│   ├── nuxt.config.ts            # Конфигурация Nuxt
│   ├── tsconfig.json             # TypeScript конфигурация
│   ├── vitest.config.ts          # Конфигурация тестов
│   ├── drizzle.config.ts         # Конфигурация Drizzle ORM
│   ├── uno.config.ts             # Конфигурация UnoCSS
│   └── .env                      # Переменные окружения
│
├── 🐳 Docker
│   ├── Dockerfile                # Production образ
│   ├── docker-compose.yml        # Production оркестрация
│   ├── docker-compose.dev.yml   # Development оркестрация
│   ├── .dockerignore             # Исключения для Docker
│   ├── Makefile                  # Удобные команды
│   └── docker/
│       └── init-db.sql           # Инициализация БД
│
├── 📱 Frontend (Nuxt 3)
│   ├── app.vue                   # Корневой компонент
│   ├── pages/                    # Страницы (file-based routing)
│   │   ├── index.vue             # Dashboard
│   │   ├── contacts.vue          # Список контактов
│   │   └── contacts/
│   │       └── [id].vue          # Детали контакта
│   │
│   ├── composables/              # Переиспользуемая логика
│   │   ├── useContacts.ts        # Управление контактами
│   │   ├── useReminders.ts       # Управление напоминаниями
│   │   ├── useGamification.ts    # Геймификация
│   │   └── useTelegramAuth.ts    # Telegram аутентификация
│   │
│   └── stores/                   # Pinia stores
│       └── auth.ts               # Состояние аутентификации
│
├── 🔧 Backend (Nitro)
│   └── server/
│       ├── api/                  # API endpoints
│       │   ├── auth.post.ts
│       │   ├── health.get.ts
│       │   ├── contacts/
│       │   │   ├── index.get.ts
│       │   │   ├── index.post.ts
│       │   │   ├── [id].get.ts
│       │   │   ├── [id].put.ts
│       │   │   └── [id].delete.ts
│       │   ├── reminders/
│       │   │   ├── index.get.ts
│       │   │   └── [id]/
│       │   │       └── complete.post.ts
│       │   ├── gamification/
│       │   │   ├── stats.get.ts
│       │   │   └── achievements.get.ts
│       │   └── webhook.post.ts
│       │
│       ├── db/                   # База данных
│       │   ├── index.ts          # Подключение
│       │   ├── schema.ts         # Схема таблиц
│       │   ├── migrate.ts        # Скрипт миграций
│       │   ├── migrations/       # Сгенерированные миграции
│       │   └── seeds/            # Начальные данные
│       │       └── achievements.ts
│       │
│       └── utils/                # Утилиты
│           ├── gamification.ts   # Логика геймификации
│           └── reminders.ts      # Логика напоминаний
│
├── 🧪 Тесты
│   └── tests/
│       └── unit/
│           ├── api/              # Тесты API
│           │   ├── auth.test.ts
│           │   ├── contacts.test.ts
│           │   ├── reminders.test.ts
│           │   └── gamification.test.ts
│           └── utils/            # Тесты утилит
│               ├── gamification.test.ts
│               └── reminders.test.ts
│
├── 📚 Документация
│   ├── README.md                 # Основной README
│   ├── БЫСТРЫЙ_СТАРТ.md         # Быстрый старт
│   ├── ЗАПУСК_ЛОКАЛЬНО.md       # Подробная инструкция
│   ├── README.Docker.md          # Docker документация
│   ├── QUICKSTART.md             # Quick start (English)
│   ├── DOCKER_SETUP_COMPLETE.md  # Docker setup guide
│   └── АРХИТЕКТУРА.md           # Этот файл
│
└── 📋 Спецификации
    └── .kiro/specs/nudge-telegram-app/
        ├── requirements.md       # Требования
        ├── design.md             # Дизайн системы
        └── tasks.md              # Задачи реализации
```

## 🔄 Поток данных

### 1. Аутентификация пользователя

```
Telegram Client
    │
    │ 1. Открывает Mini App
    ▼
Nuxt Frontend
    │
    │ 2. Получает initData (signed)
    ▼
POST /api/auth
    │
    │ 3. Валидирует подпись HMAC-SHA256
    ▼
PostgreSQL
    │
    │ 4. Создает/получает пользователя
    ▼
Nuxt Frontend
    │
    │ 5. Сохраняет сессию в Pinia
    ▼
Dashboard
```

### 2. Управление контактами

```
User Action (Contacts Page)
    │
    │ 1. Запрос списка контактов
    ▼
useContacts composable
    │
    │ 2. fetchContacts()
    ▼
GET /api/contacts?userId=X
    │
    │ 3. Drizzle ORM query
    ▼
PostgreSQL (contacts table)
    │
    │ 4. Возвращает данные
    ▼
Nuxt Frontend
    │
    │ 5. Отображает список с фильтрами
    ▼
User sees contacts
```

### 3. Система напоминаний

```
Node-cron Scheduler (Daily)
    │
    │ 1. Запускается каждый день
    ▼
Calculate Reminders
    │
    │ 2. Находит контакты, требующие напоминания
    ▼
PostgreSQL (contacts, reminders)
    │
    │ 3. Создает записи напоминаний
    ▼
Telegram Bot API
    │
    │ 4. Отправляет уведомления
    ▼
User receives notification
    │
    │ 5. Открывает Mini App
    ▼
Dashboard shows reminders
    │
    │ 6. Отмечает как выполненное
    ▼
POST /api/reminders/:id/complete
    │
    │ 7. Обновляет БД + начисляет XP
    ▼
Gamification system updates
```

## 🎮 Система геймификации

```
User Action (Complete Reminder)
    │
    ▼
POST /api/reminders/:id/complete
    │
    ├─► Update reminder status
    │
    ├─► Update contact.lastContactDate
    │
    ├─► Award XP (20 points)
    │   └─► Check level up
    │
    ├─► Update streak
    │   ├─► If consecutive day: increment
    │   └─► If missed day: reset to 1
    │
    ├─► Log activity
    │   └─► activity_logs table
    │
    └─► Check achievements
        ├─► First reminder completed
        ├─► 10 reminders completed
        ├─► 7-day streak
        └─► etc.
```

## 🔐 Безопасность

### Аутентификация

```
Telegram initData
    │
    │ Contains: user_id, username, auth_date, hash
    ▼
Backend Validation
    │
    ├─► Verify HMAC-SHA256 signature
    │   └─► Using TELEGRAM_BOT_TOKEN
    │
    ├─► Check auth_date (not too old)
    │
    └─► Extract user info
        └─► Create/update user in DB
```

### API Security

- ✅ Все запросы требуют userId
- ✅ Проверка принадлежности данных пользователю
- ✅ Валидация входных данных
- ✅ SQL injection защита (Drizzle ORM)
- ✅ XSS защита (Vue автоматически)

## 📊 База данных

### Схема таблиц

```sql
users
├── id (PK)
├── telegramId (unique)
├── username
├── firstName
├── lastName
├── currentStreak
├── longestStreak
├── totalXP
├── level
├── lastActivityDate
├── createdAt
└── updatedAt

contacts
├── id (PK)
├── userId (FK → users.id)
├── telegramContactId
├── name
├── username
├── isTracked
├── frequency
├── customFrequencyDays
├── communicationType
├── category
├── lastContactDate
├── nextReminderDate
├── createdAt
└── updatedAt

reminders
├── id (PK)
├── userId (FK → users.id)
├── contactId (FK → contacts.id)
├── dueDate
├── completed
├── completedAt
├── notificationSent
└── createdAt

achievements
├── id (PK)
├── code (unique)
├── name
├── description
├── icon
├── xpReward
└── criteria (jsonb)

user_achievements
├── id (PK)
├── userId (FK → users.id)
├── achievementId (FK → achievements.id)
└── unlockedAt

activity_logs
├── id (PK)
├── userId (FK → users.id)
├── action
├── xpAwarded
├── metadata (jsonb)
└── createdAt
```

## 🚀 Deployment Flow

### Development

```
Local Machine
    │
    │ make dev
    ▼
Docker Compose (dev)
    ├─► PostgreSQL container
    ├─► Nuxt dev server (hot-reload)
    └─► Adminer container
```

### Production

```
Build Server
    │
    │ make build
    ▼
Docker Image
    │
    │ Multi-stage build
    ├─► Stage 1: Install deps
    ├─► Stage 2: Build app
    └─► Stage 3: Production runtime
    │
    ▼
Docker Registry
    │
    ▼
Production Server
    │
    │ docker-compose up
    ▼
Running Containers
    ├─► Nuxt app (optimized)
    ├─► PostgreSQL (persistent)
    └─► Nginx reverse proxy (optional)
```

## 📈 Масштабирование

### Горизонтальное масштабирование

```
Load Balancer
    │
    ├─► Nuxt App Instance 1
    ├─► Nuxt App Instance 2
    └─► Nuxt App Instance 3
         │
         └─► Shared PostgreSQL
              └─► Read Replicas (optional)
```

### Кэширование

```
User Request
    │
    ▼
Redis Cache (optional)
    │
    ├─► Cache Hit → Return data
    │
    └─► Cache Miss
         │
         ▼
    PostgreSQL
         │
         └─► Store in cache
```

---

**Эта архитектура обеспечивает:**
- ✅ Масштабируемость
- ✅ Безопасность
- ✅ Производительность
- ✅ Простоту разработки
- ✅ Легкость деплоя
