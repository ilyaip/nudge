# ⚡ Быстрый старт - Копируй и вставляй команды

## 🐳 С Docker (30 секунд)

```bash
# 1. Перейди в папку проекта
cd /путь/к/nudge-telegram-app

# 2. Запусти всё одной командой
make quickstart

# 3. Открой в браузере
# http://localhost:3000
```

**Готово!** 🎉

---

## 💻 Без Docker (5 минут)

### macOS:

```bash
# 1. Установи PostgreSQL
brew install postgresql@15
brew services start postgresql@15

# 2. Создай базу данных
createdb nudge

# 3. Установи зависимости
npm install

# 4. Настрой .env
cp .env.example .env
# Отредактируй .env:
# DATABASE_URL=postgresql://твой_username@localhost:5432/nudge

# 5. Выполни миграции
npm run db:migrate

# 6. Запусти приложение
npm run dev

# 7. Открой в браузере
# http://localhost:3000
```

### Windows:

```bash
# 1. Установи PostgreSQL с https://www.postgresql.org/download/windows/
# 2. Создай базу данных "nudge" через pgAdmin

# 3. Установи зависимости
npm install

# 4. Настрой .env
copy .env.example .env
# Отредактируй .env:
# DATABASE_URL=postgresql://postgres:твой_пароль@localhost:5432/nudge

# 5. Выполни миграции
npm run db:migrate

# 6. Запусти приложение
npm run dev

# 7. Открой в браузере
# http://localhost:3000
```

### Linux (Ubuntu/Debian):

```bash
# 1. Установи PostgreSQL
sudo apt-get update
sudo apt-get install postgresql-15
sudo systemctl start postgresql

# 2. Создай базу данных
sudo -u postgres createdb nudge

# 3. Установи зависимости
npm install

# 4. Настрой .env
cp .env.example .env
# Отредактируй .env:
# DATABASE_URL=postgresql://postgres:твой_пароль@localhost:5432/nudge

# 5. Выполни миграции
npm run db:migrate

# 6. Запусти приложение
npm run dev

# 7. Открой в браузере
# http://localhost:3000
```

---

## 🔧 Если что-то не работает

### Порт занят:
```bash
lsof -i :3000
kill -9 <PID>
```

### БД не подключается:
```bash
# С Docker
make clean
make quickstart

# Без Docker
brew services restart postgresql@15  # macOS
sudo systemctl restart postgresql    # Linux
```

### Ошибки при установке:
```bash
rm -rf node_modules
npm install
```

---

## 📚 Полная документация

- **Подробная инструкция:** [ЗАПУСК_ЛОКАЛЬНО.md](./ЗАПУСК_ЛОКАЛЬНО.md)
- **Docker документация:** [README.Docker.md](./README.Docker.md)
- **Основной README:** [README.md](./README.md)

---

## ✅ Проверка

После запуска открой:
- http://localhost:3000 - Приложение
- http://localhost:8080 - Adminer (только с Docker)

Если видишь Dashboard - всё работает! 🎉
