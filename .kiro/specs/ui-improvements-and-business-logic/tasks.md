# Implementation Plan: UI Improvements and Business Logic Fixes

## Overview

Этот план включает исправление бизнес-логики геймификации и полное обновление дизайна приложения в соответствии с современным стилем E-Wallet. Задачи разделены на фазы для удобства выполнения.

## Tasks

### Фаза 1: Исправление бизнес-логики геймификации

- [x] 1. Исправить API endpoint для завершения напоминания
  - Открыть `server/api/reminders/[id]/complete.post.ts`
  - После обновления напоминания и контакта, добавить вызовы:
    - `await updateStreak(user.id)` - обновить streak
    - `await awardXP(user.id, 'REMINDER_COMPLETED')` - начислить 20 XP
    - `const newAchievements = await checkAchievements(user.id)` - проверить достижения
  - Получить обновленные данные пользователя из БД
  - Вернуть в ответе: `xpEarned`, `newLevel`, `newStreak`, `unlockedAchievements`
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5_

- [x] 2. Обновить фронтенд для отображения заработанного XP
  - Открыть `composables/useReminders.ts`
  - В функции `completeReminder` получить данные из ответа API
  - Показать уведомление с заработанным XP: `showSuccess('+{xp} XP заработано!', '🎉 Отлично!')`
  - Если разблокированы достижения - показать отдельное уведомление для каждого
  - _Requirements: 2.4_

- [x] 3. Добавить автообновление статистики после завершения
  - В `pages/index.vue` в функции `handleCompleteReminder`
  - После `await completeReminder(reminderId)` вызвать `await fetchGamification()`
  - Убедиться, что XP, level, streak обновляются на UI
  - _Requirements: 2.1, 2.2, 2.3_

- [x] 4. Checkpoint - Протестировать геймификацию
  - Открыть Dashboard в браузере
  - Завершить напоминание
  - Проверить:
    - Показалось уведомление с XP?
    - XP обновился на Dashboard?
    - Уровень обновился?
    - Streak обновился?
    - Напоминание исчезло из списка?
  - Проверить в БД, что создалась запись в `activity_logs`

### Фаза 2: Создание новой цветовой схемы

- [x] 5. Создать файл с цветовыми константами
  - Создать `assets/styles/colors.ts` (или добавить в UnoCSS config)
  - Определить цвета:
    ```typescript
    export const colors = {
      primary: '#6B3CE9',      // Основной фиолетовый
      primaryDark: '#1A0B2E',  // Темно-фиолетовый
      primaryLight: '#8B5CF6', // Светло-фиолетовый
      background: '#FFFFFF',   // Белый фон
      backgroundSecondary: '#F8F9FA', // Вторичный фон
      text: '#1F2937',         // Основной текст
      textSecondary: '#6B7280' // Вторичный текст
    }
    ```
  - Если используется UnoCSS, добавить эти цвета в `uno.config.ts`
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [x] 6. Обновить UnoCSS конфигурацию
  - Открыть `uno.config.ts`
  - Добавить кастомные цвета в theme.colors
  - Добавить утилиты для градиентов:
    - `gradient-purple-dark`: от #1A0B2E к #2D1B4E
    - `gradient-purple-bright`: от #6B3CE9 к #8B5CF6
  - _Requirements: 3.1, 3.2, 3.3_

### Фаза 3: Обновление Dashboard

- [x] 7. Создать новый header с градиентом
  - Открыть `pages/index.vue`
  - Заменить белый header на фиолетовый градиент
  - Структура:
    ```vue
    <header class="bg-gradient-to-br from-primary to-primaryLight p-6 rounded-b-3xl mb-6">
      <div class="flex items-center justify-between mb-4">
        <div class="w-12 h-12 rounded-full bg-white/20"></div> <!-- Аватар -->
        <button class="w-10 h-10 rounded-full bg-white/20"></button> <!-- Уведомления -->
      </div>
      <p class="text-white/80 text-sm mb-2">Ваш прогресс</p>
      <p class="text-white text-4xl font-bold">{{ totalXP }} XP</p>
      <p class="text-white/80 text-sm mt-1">Уровень {{ level }}</p>
    </header>
    ```
  - _Requirements: 4.1, 4.3_

- [x] 8. Обновить карточки статистики с градиентами
  - В `pages/index.vue` найти секцию со статистикой
  - Заменить на две карточки с градиентами:
    ```vue
    <div class="grid grid-cols-2 gap-4 mb-6">
      <!-- Streak - темная карточка -->
      <div class="bg-gradient-purple-dark rounded-3xl p-6 text-white relative overflow-hidden">
        <div class="absolute top-4 right-4 w-12 h-12 rounded-full bg-white/10 flex items-center justify-center">
          <span class="text-2xl">🔥</span>
        </div>
        <p class="text-white/70 text-sm mb-2">Текущий стрик</p>
        <p class="text-4xl font-bold">{{ currentStreak }}</p>
        <p class="text-white/70 text-xs mt-1">дней подряд</p>
      </div>
      
      <!-- Level - яркая карточка -->
      <div class="bg-gradient-purple-bright rounded-3xl p-6 text-white relative overflow-hidden">
        <div class="absolute top-4 right-4 w-12 h-12 rounded-full bg-white/20 flex items-center justify-center">
          <span class="text-2xl">⭐</span>
        </div>
        <p class="text-white/80 text-sm mb-2">Уровень</p>
        <p class="text-4xl font-bold">{{ level }}</p>
        <p class="text-white/80 text-xs mt-1">{{ totalXP }} XP</p>
      </div>
    </div>
    ```
  - Увеличить border-radius до 24px (rounded-3xl)
  - _Requirements: 4.2, 4.5_

- [x] 9. Улучшить карточки напоминаний
  - В `pages/index.vue` найти секцию с напоминаниями
  - Добавить круглые аватары/иконки:
    ```vue
    <div class="flex items-center gap-4">
      <!-- Аватар или иконка категории -->
      <div class="w-14 h-14 rounded-full bg-gradient-purple-bright flex items-center justify-center text-2xl flex-shrink-0">
        {{ getCategoryIcon(reminder.contact?.category) }}
      </div>
      
      <!-- Информация -->
      <div class="flex-1">
        <h3 class="text-lg font-bold text-gray-900">{{ reminder.contact?.name }}</h3>
        <p class="text-sm text-gray-500">{{ getCategoryLabel(reminder.contact?.category) }}</p>
      </div>
      
      <!-- Кнопка -->
      <button class="bg-primary hover:bg-primaryLight text-white px-6 py-3 rounded-2xl font-medium">
        ✓ Готово
      </button>
    </div>
    ```
  - Увеличить размер шрифта для имен до 18px
  - _Requirements: 5.1, 5.2, 5.3, 5.4_

- [x] 10. Добавить анимацию исчезновения напоминания
  - В `composables/useReminders.ts` или `pages/index.vue`
  - Добавить CSS transition для fade-out:
    ```vue
    <transition name="fade">
      <div v-for="reminder in todayReminders" :key="reminder.id">
        <!-- карточка напоминания -->
      </div>
    </transition>
    
    <style scoped>
    .fade-leave-active {
      transition: opacity 0.3s ease, transform 0.3s ease;
    }
    .fade-leave-to {
      opacity: 0;
      transform: translateX(20px);
    }
    </style>
    ```
  - _Requirements: 5.5, 9.2_

### Фаза 4: Обновление страницы контактов

- [x] 11. Обновить дизайн списка контактов
  - Открыть `pages/contacts/index.vue`
  - Добавить круглые аватары (56px):
    ```vue
    <div class="w-14 h-14 rounded-full bg-gradient-purple-bright flex items-center justify-center text-2xl flex-shrink-0">
      {{ getCategoryIcon(contact.category) }}
    </div>
    ```
  - Увеличить размер шрифта для имен до 16px, сделать bold
  - Использовать белый фон с тенью для карточек
  - Вторичную информацию показывать серым цветом (#6B7280)
  - _Requirements: 6.1, 6.2, 6.3, 6.4_

- [x] 12. Обновить кнопку "Добавить"
  - Изменить цвет на фиолетовый (bg-primary)
  - Увеличить border-radius до 16px
  - Добавить hover эффект (bg-primaryLight)
  - _Requirements: 6.5_

- [x] 13. Обновить страницу деталей контакта
  - Открыть `pages/contacts/[id].vue`
  - Добавить круглый аватар/иконку в header (80px)
  - Использовать фиолетовый цвет для кнопки "Сохранить"
  - Увеличить border-radius для всех карточек до 20px
  - _Requirements: 6.1, 6.2_

### Фаза 5: Обновление страницы достижений

- [x] 14. Улучшить карточки разблокированных достижений
  - Открыть `pages/achievements.vue`
  - Для разблокированных достижений использовать градиент:
    ```vue
    <div class="bg-gradient-to-br from-yellow-400 to-orange-500 rounded-3xl p-6 text-white">
      <div class="flex items-start gap-4">
        <div class="text-5xl">{{ achievement.icon }}</div>
        <div class="flex-1">
          <h3 class="text-xl font-bold">{{ achievement.name }}</h3>
          <p class="text-white/90 text-sm mt-1">{{ achievement.description }}</p>
          <div class="mt-3 flex items-center gap-2">
            <span class="bg-white/20 px-3 py-1 rounded-full text-sm">+{{ achievement.xpReward }} XP</span>
            <span class="text-sm text-white/80">{{ formatDate(achievement.unlockedAt) }}</span>
          </div>
        </div>
      </div>
    </div>
    ```
  - _Requirements: 7.1, 7.5_

- [x] 15. Улучшить карточки заблокированных достижений
  - Использовать grayscale filter и opacity 0.6
  - Добавить фиолетовый прогресс-бар:
    ```vue
    <div class="mt-3">
      <div class="flex justify-between text-xs mb-1">
        <span>Прогресс</span>
        <span>{{ achievement.progress }}%</span>
      </div>
      <div class="w-full bg-gray-200 rounded-full h-2">
        <div 
          class="bg-gradient-to-r from-primary to-primaryLight h-2 rounded-full transition-all duration-500"
          :style="{ width: `${achievement.progress}%` }"
        ></div>
      </div>
    </div>
    ```
  - _Requirements: 7.2, 7.3_

- [x] 16. Добавить celebration анимацию для новых достижений
  - Создать компонент `components/AchievementUnlockModal.vue`
  - Показывать модальное окно с анимацией при разблокировке
  - Использовать confetti или sparkle эффект
  - Автоматически закрывать через 3 секунды
  - _Requirements: 7.4, 10.4_

### Фаза 6: Улучшение графика активности

- [x] 17. Обновить компонент ActivityChart
  - Открыть `components/ActivityChart.vue`
  - Изменить цвет столбцов на фиолетовый градиент
  - Добавить hover эффект с tooltip:
    ```vue
    <div 
      @mouseenter="showTooltip(day)" 
      @mouseleave="hideTooltip"
      class="relative group"
    >
      <div class="bg-gradient-to-t from-primary to-primaryLight rounded-t-lg transition-all duration-300 group-hover:scale-105">
        <!-- столбец -->
      </div>
      <div v-if="hoveredDay === day" class="absolute bottom-full mb-2 bg-gray-900 text-white px-3 py-2 rounded-lg text-sm">
        {{ day.completedReminders }} напоминаний<br>
        {{ day.xpEarned }} XP
      </div>
    </div>
    ```
  - _Requirements: 8.1, 8.2_

- [x] 18. Добавить анимацию роста столбцов
  - Использовать CSS transitions для высоты столбцов
  - При обновлении данных анимировать изменение высоты (500ms)
  - _Requirements: 8.3, 9.4_

- [x] 19. Добавить цветовое кодирование по интенсивности
  - Использовать разные оттенки фиолетового в зависимости от активности:
    - Высокая активность (>5 напоминаний): темный фиолетовый
    - Средняя активность (2-5): средний фиолетовый
    - Низкая активность (1): светлый фиолетовый
    - Нет активности: серый
  - _Requirements: 8.4, 8.5_

### Фаза 7: Улучшение уведомлений

- [x] 20. Обновить компонент NotificationToast
  - Открыть `components/NotificationToast.vue`
  - Для success: использовать зеленый фон (#10B981) с белым текстом
  - Для error: использовать красный фон (#EF4444) с белым текстом
  - Для XP: добавить иконку ✨ и фиолетовый фон
  - Увеличить border-radius до 16px
  - _Requirements: 10.1, 10.2, 10.3_

- [x] 21. Добавить автозакрытие уведомлений
  - В `composables/useNotifications.ts`
  - Добавить таймер на 3 секунды для автозакрытия
  - Добавить анимацию появления/исчезновения
  - _Requirements: 10.5_

### Фаза 8: Добавление общих анимаций

- [x] 22. Создать глобальные CSS transitions
  - Создать файл `assets/styles/transitions.css`
  - Добавить transitions:
    ```css
    .fade-enter-active, .fade-leave-active {
      transition: opacity 0.3s ease;
    }
    .fade-enter-from, .fade-leave-to {
      opacity: 0;
    }
    
    .scale-enter-active, .scale-leave-active {
      transition: transform 0.3s ease, opacity 0.3s ease;
    }
    .scale-enter-from, .scale-leave-to {
      transform: scale(0.95);
      opacity: 0;
    }
    ```
  - Импортировать в `app.vue`
  - _Requirements: 9.1, 9.2_

- [x] 23. Добавить hover эффекты для интерактивных элементов
  - Для кнопок: добавить `hover:scale-105 transition-transform`
  - Для карточек: добавить `hover:shadow-lg transition-shadow`
  - Для ссылок: добавить `hover:bg-gray-50 transition-colors`
  - _Requirements: 9.3_

- [x] 24. Добавить ripple эффект для кнопок
  - Создать директиву `v-ripple` или использовать готовую библиотеку
  - Применить к кнопкам "Готово", "Добавить", "Сохранить"
  - _Requirements: 9.5_

### Фаза 9: Обновление нижней навигации

- [x] 25. Обновить стиль BottomNav
  - Открыть `components/BottomNav.vue`
  - Изменить активный цвет на фиолетовый (text-primary)
  - Добавить индикатор активной страницы (маленький кружок сверху)
  - Увеличить размер иконок до 24px
  - Добавить smooth transition при переключении

### Фаза 10: Финальная проверка и тестирование

- [x] 26. Полное тестирование геймификации
  - Создать нового пользователя
  - Добавить контакты
  - Завершить несколько напоминаний
  - Проверить:
    - XP начисляется?
    - Уровень растет?
    - Streak обновляется?
    - Достижения разблокируются?
    - График активности обновляется?

- [x] 27. Проверка дизайна на всех страницах
  - Dashboard: header, карточки, напоминания
  - Contacts: список, детали, добавление
  - Achievements: разблокированные, заблокированные, прогресс
  - Проверить на разных размерах экрана (mobile, tablet)

- [x] 28. Проверка анимаций и transitions
  - Завершение напоминания - fade out
  - Появление уведомлений - fade in
  - Hover эффекты на карточках
  - Рост столбцов графика
  - Разблокировка достижения - celebration

- [x] 29. Финальный checkpoint
  - Запустить все тесты: `npm test`
  - Проверить TypeScript: `npm run type-check`
  - Проверить производительность в DevTools
  - Убедиться, что нет console.error
  - Создать список найденных проблем (если есть)

## Notes

- Все изменения должны быть совместимы с Telegram Mini App
- Цвета должны быть доступны через UnoCSS утилиты
- Анимации должны быть плавными (не более 500ms)
- Все тексты на русском языке
- Использовать существующие компоненты где возможно
- Тестировать на реальном устройстве через Telegram
