# Деплой на Render.com

Это руководство поможет вам развернуть Nudge Telegram Mini App на Render.com и протестировать в Telegram.

## Предварительные требования

1. ✅ Аккаунт на [Render.com](https://render.com) (можно войти через GitHub)
2. ✅ Репозиторий на GitHub с вашим кодом
3. ✅ Telegram Bot Token (уже есть в `.env`)

## Шаг 1: Подготовка репозитория

### 1.1 Инициализируйте Git (если еще не сделали)

```bash
git init
git add .
git commit -m "Initial commit: Nudge Telegram Mini App"
```

### 1.2 Создайте репозиторий на GitHub

1. Перейдите на [github.com/new](https://github.com/new)
2. Создайте новый репозиторий (например, `nudge-telegram-app`)
3. **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)

### 1.3 Загрузите код на GitHub

```bash
git remote add origin https://github.com/ВАШ_USERNAME/nudge-telegram-app.git
git branch -M main
git push -u origin main
```

## Шаг 2: Создание проекта на Render

### 2.1 Войдите в Render

1. Перейдите на [dashboard.render.com](https://dashboard.render.com)
2. Войдите через GitHub
3. Разрешите Render доступ к вашим репозиториям

### 2.2 Создайте Blueprint (автоматический деплой)

1. Нажмите **"New +"** → **"Blueprint"**
2. Выберите ваш репозиторий `nudge-telegram-app`
3. Render автоматически обнаружит файл `render.yaml`
4. Нажмите **"Apply"**

Render создаст:
- ✅ Web Service (Nuxt приложение)
- ✅ PostgreSQL базу данных
- ✅ Автоматическое подключение между ними

### 2.3 Добавьте Telegram Bot Token

1. Перейдите в созданный Web Service
2. Откройте вкладку **"Environment"**
3. Найдите переменную `TELEGRAM_BOT_TOKEN`
4. Вставьте ваш токен: `8153958316:AAFbAXd5oHJtSGZ7kL-BV4_eCz_1WgPISK4`
5. Нажмите **"Save Changes"**

Приложение автоматически перезапустится.

## Шаг 3: Дождитесь завершения деплоя

### 3.1 Мониторинг деплоя

1. Откройте вкладку **"Logs"** в вашем Web Service
2. Дождитесь сообщения: `✔ Nuxt Nitro server built`
3. Деплой занимает 5-10 минут при первом запуске

### 3.2 Проверьте статус

1. Откройте вкладку **"Events"**
2. Убедитесь, что статус: **"Deploy live"** (зеленый)
3. Скопируйте URL вашего приложения (например, `https://nudge-telegram-app.onrender.com`)

## Шаг 4: Запуск миграций базы данных

### 4.1 Подключитесь к базе данных

1. Перейдите в **PostgreSQL** сервис в Render Dashboard
2. Откройте вкладку **"Info"**
3. Скопируйте **"External Database URL"**

### 4.2 Запустите миграции локально

```bash
# Установите переменную окружения
export DATABASE_URL="postgresql://nudge_user:PASSWORD@HOST:5432/nudge"

# Запустите миграции
npm run db:migrate
```

**Альтернатива:** Используйте Render Shell (платная функция) или подключитесь через Adminer.

## Шаг 5: Настройка Telegram Bot

### 5.1 Откройте BotFather в Telegram

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду: `/mybots`
3. Выберите вашего бота

### 5.2 Настройте Web App URL

1. Нажмите **"Bot Settings"** → **"Menu Button"**
2. Выберите **"Configure menu button"**
3. Введите URL: `https://nudge-telegram-app.onrender.com`
4. Введите название кнопки: `Открыть Nudge`

### 5.3 Настройте описание (опционально)

```
/setdescription
Nudge помогает поддерживать важные социальные связи через умные напоминания
```

## Шаг 6: Тестирование в Telegram

### 6.1 Откройте бота

1. Найдите вашего бота в Telegram
2. Нажмите **"Start"** или `/start`
3. Нажмите кнопку **"Открыть Nudge"** в меню

### 6.2 Проверьте функциональность

- ✅ Приложение открывается в Telegram
- ✅ Авторизация через Telegram работает
- ✅ Можно просматривать контакты
- ✅ Можно добавлять/редактировать контакты

## Важные замечания

### ⚠️ Ограничения бесплатного тарифа

1. **Холодный старт**: Приложение "засыпает" после 15 минут неактивности
   - Первый запрос после сна занимает ~30 секунд
   - Это нормально для тестирования

2. **PostgreSQL**: Бесплатная база работает 90 дней
   - После этого нужно перейти на платный тариф ($7/мес)
   - Или использовать внешнюю БД (Supabase, Neon)

3. **Лимиты**: 750 часов работы в месяц
   - Достаточно для тестирования и небольшого использования

### 🚀 Переход на Production

Для реального использования рекомендуется:

1. **Платный тариф Render**: $7/мес за Web Service
   - Нет холодного старта
   - Больше ресурсов
   - Лучшая производительность

2. **Managed PostgreSQL**: $7/мес
   - Автоматические бэкапы
   - Высокая доступность

3. **Custom Domain**: Подключите свой домен
   - Бесплатный SSL сертификат
   - Профессиональный вид

## Мониторинг и отладка

### Просмотр логов

```bash
# В Render Dashboard
Web Service → Logs → Live Logs
```

### Проверка здоровья приложения

```bash
curl https://nudge-telegram-app.onrender.com/api/health
```

Должен вернуть:
```json
{
  "status": "ok",
  "timestamp": "2026-01-02T..."
}
```

### Подключение к базе данных

Используйте **External Database URL** из Render Dashboard:

```bash
psql "postgresql://nudge_user:PASSWORD@HOST:5432/nudge"
```

## Автоматические обновления

### Continuous Deployment

Render автоматически деплоит изменения при push в `main`:

```bash
git add .
git commit -m "Update feature"
git push origin main
```

Render автоматически:
1. Обнаружит изменения
2. Соберет новый Docker образ
3. Запустит health check
4. Переключит трафик на новую версию

## Troubleshooting

### Проблема: Приложение не запускается

**Решение:**
1. Проверьте логи в Render Dashboard
2. Убедитесь, что `TELEGRAM_BOT_TOKEN` установлен
3. Проверьте, что база данных создана и доступна

### Проблема: База данных не подключается

**Решение:**
1. Проверьте, что `DATABASE_URL` автоматически установлен
2. Убедитесь, что миграции выполнены
3. Проверьте логи PostgreSQL сервиса

### Проблема: Telegram не открывает приложение

**Решение:**
1. Убедитесь, что URL в BotFather правильный
2. Проверьте, что приложение доступно по HTTPS
3. Очистите кэш Telegram (Settings → Data and Storage → Clear Cache)

## Полезные ссылки

- [Render Documentation](https://render.com/docs)
- [Telegram Mini Apps Guide](https://core.telegram.org/bots/webapps)
- [Nuxt Deployment](https://nuxt.com/docs/getting-started/deployment)

## Поддержка

Если возникли проблемы:
1. Проверьте логи в Render Dashboard
2. Проверьте статус сервисов
3. Обратитесь к документации Render

---

**Готово!** Ваше приложение теперь доступно в Telegram! 🎉
