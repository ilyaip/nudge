# Планировщик уведомлений Nudge

## Обзор

Система планировщика автоматически рассчитывает и отправляет напоминания пользователям о необходимости связаться с контактами.

## Компоненты

### 1. Scheduler (`server/utils/scheduler.ts`)

Основной модуль планировщика, отвечающий за:
- Расчет напоминаний для всех пользователей
- Создание записей напоминаний в БД
- Генерацию запросов на уведомления
- Инициализацию cron-задачи

**Основные функции:**

- `calculateRemindersForAllUsers()` - Рассчитывает напоминания для всех пользователей
- `scheduledReminderTask()` - Основная задача планировщика (расчет + отправка)
- `initializeScheduler()` - Инициализирует cron-задачу (запуск каждый день в 9:00 UTC)
- `runSchedulerManually()` - Ручной запуск для тестирования

### 2. Telegram Bot (`server/utils/telegram-bot.ts`)

Модуль для работы с Telegram Bot API:
- Отправка сообщений пользователям
- Форматирование сообщений с напоминаниями
- Логика повторных попыток при сбоях
- Логирование событий отправки

**Основные функции:**

- `sendNotifications()` - Отправляет уведомления списку пользователей
- `TelegramBot.sendMessage()` - Отправляет одно сообщение
- `TelegramBot.sendMessageWithRetry()` - Отправка с повторными попытками
- `TelegramBot.formatReminderMessage()` - Форматирует сообщение

### 3. Nitro Plugin (`server/plugins/scheduler.ts`)

Плагин для автоматической инициализации планировщика при запуске сервера.

## Конфигурация

### Переменные окружения

```env
# Токен Telegram бота (обязательно)
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Включить планировщик (опционально, по умолчанию false в dev)
ENABLE_SCHEDULER=true
```

### Расписание

По умолчанию планировщик запускается **каждый день в 9:00 UTC**.

Для изменения расписания отредактируйте cron-выражение в `initializeScheduler()`:

```typescript
const cronExpression = '0 9 * * *' // минута час день месяц день_недели
```

Примеры:
- `0 9 * * *` - каждый день в 9:00
- `0 */6 * * *` - каждые 6 часов
- `0 9 * * 1-5` - каждый будний день в 9:00

## API Endpoints

### Ручной запуск планировщика

```http
POST /api/scheduler/run
```

Запускает задачу планировщика вручную (для тестирования).

**Ответ:**
```json
{
  "success": true,
  "message": "Планировщик успешно выполнен",
  "notificationRequestsCount": 5,
  "notificationRequests": [...]
}
```

### Тестовое уведомление

```http
POST /api/notifications/test
Content-Type: application/json

{
  "telegramId": "123456789",
  "message": "Тестовое сообщение" // опционально
}
```

Отправляет тестовое уведомление указанному пользователю.

**Ответ:**
```json
{
  "success": true,
  "message": "Уведомление успешно отправлено",
  "retryCount": 0
}
```

## Логика работы

### 1. Расчет напоминаний

Для каждого пользователя:
1. Получить все отслеживаемые контакты (`isTracked = true`)
2. Для каждого контакта проверить, требуется ли напоминание сегодня
3. Создать запись в таблице `reminders` если напоминание требуется
4. Сгенерировать запрос на уведомление

### 2. Отправка уведомлений

Для каждого запроса на уведомление:
1. Форматировать сообщение со списком контактов
2. Отправить сообщение через Telegram Bot API
3. При сбое - повторить до 3 раз с экспоненциальной задержкой
4. Обновить статус `notificationSent` в БД
5. Логировать результат

### 3. Обработка ошибок

- **Сбой отправки**: До 3 повторных попыток с задержкой (1с, 2с, 4с)
- **Ошибка БД**: Логирование и пропуск пользователя
- **Отсутствие токена**: Выброс исключения при инициализации

## Формат уведомлений

Пример сообщения:

```
🔔 Напоминание о контактах

Сегодня стоит связаться с 2 контактами:

1. 👥 Иван Петров (@ivan)
   📅 Частота: Еженедельно
   💬 Тип: Сообщение

2. 💼 Мария Сидорова
   📅 Частота: Ежемесячно
   💬 Тип: Звонок

✅ Отметьте выполненные напоминания в приложении!
```

## Мониторинг

Все события логируются с префиксом:
- `[Scheduler]` - события планировщика
- `[Telegram Bot]` - события отправки уведомлений
- `[Nitro Plugin]` - события инициализации

Примеры логов:

```
[Scheduler] Начало расчета напоминаний для всех пользователей
[Scheduler] Найдено пользователей: 10
[Scheduler] Пользователь 123456789: найдено 2 контактов для напоминания
[Telegram Bot] Уведомление отправлено пользователю 123456789 (попыток: 1)
[Scheduler] Задача завершена. Отправлено уведомлений: 8/10
```

## Тестирование

### Локальное тестирование

1. Установите `ENABLE_SCHEDULER=true` в `.env`
2. Запустите сервер: `npm run dev`
3. Или используйте ручной запуск: `POST /api/scheduler/run`

### Тестирование отправки

```bash
curl -X POST http://localhost:3000/api/notifications/test \
  -H "Content-Type: application/json" \
  -d '{"telegramId": "YOUR_TELEGRAM_ID"}'
```

## Производственное развертывание

1. Убедитесь, что `TELEGRAM_BOT_TOKEN` установлен
2. Установите `ENABLE_SCHEDULER=true` или `NODE_ENV=production`
3. Планировщик запустится автоматически при старте сервера
4. Проверьте логи для подтверждения инициализации

## Масштабирование

При горизонтальном масштабировании:
- Включите планировщик только на одном инстансе
- Или используйте внешний планировщик (AWS EventBridge, Cloud Scheduler)
- Или используйте распределенные блокировки (Redis)

## Troubleshooting

**Планировщик не запускается:**
- Проверьте `ENABLE_SCHEDULER=true` в `.env`
- Проверьте логи при старте сервера

**Уведомления не отправляются:**
- Проверьте `TELEGRAM_BOT_TOKEN` в `.env`
- Проверьте, что бот имеет права отправлять сообщения
- Проверьте логи на ошибки API

**Дублирование напоминаний:**
- Система проверяет существующие напоминания перед созданием
- Если проблема сохраняется, проверьте логику в `calculateRemindersForAllUsers()`
