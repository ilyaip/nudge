# 🚀 Пошаговая инструкция: Запуск приложения локально

## Вариант 1: С Docker (РЕКОМЕНДУЕТСЯ) ⭐

Это самый простой способ - всё работает из коробки!

### Шаг 1: Проверь, что Docker установлен

Открой терминал и выполни:

```bash
docker --version
docker-compose --version
```

Если видишь версии - отлично! Если нет - [установи Docker Desktop](https://www.docker.com/products/docker-desktop/).

### Шаг 2: Перейди в папку проекта

```bash
cd /путь/к/проекту/nudge-telegram-app
```

### Шаг 3: Запусти приложение

```bash
make quickstart
```

**Вот и всё!** 🎉

Команда `make quickstart` автоматически:
- ✅ Соберет Docker образы
- ✅ Запустит PostgreSQL базу данных
- ✅ Запустит Nuxt приложение
- ✅ Выполнит миграции базы данных
- ✅ Откроет приложение на http://localhost:3000

### Шаг 4: Открой в браузере

- **Приложение:** http://localhost:3000
- **Adminer (управление БД):** http://localhost:8080

### Что делать дальше?

```bash
# Посмотреть логи
make logs

# Остановить приложение
make down

# Перезапустить
make restart

# Посмотреть все команды
make help
```

---

## Вариант 2: Без Docker (для разработчиков)

Если хочешь запустить без Docker, нужно больше шагов.

### Предварительные требования

Убедись, что установлено:
- ✅ Node.js 20+ ([скачать](https://nodejs.org/))
- ✅ PostgreSQL 15+ ([скачать](https://www.postgresql.org/download/))
- ✅ npm или yarn

### Шаг 1: Установи зависимости

```bash
npm install
```

Это установит все необходимые пакеты из `package.json`.

### Шаг 2: Настрой PostgreSQL

#### На macOS (с Homebrew):

```bash
# Установить PostgreSQL
brew install postgresql@15

# Запустить PostgreSQL
brew services start postgresql@15

# Создать базу данных
createdb nudge
```

#### На Windows:

1. Скачай и установи [PostgreSQL](https://www.postgresql.org/download/windows/)
2. Запусти pgAdmin
3. Создай новую базу данных с именем `nudge`

#### На Linux:

```bash
# Ubuntu/Debian
sudo apt-get install postgresql-15

# Запустить PostgreSQL
sudo systemctl start postgresql

# Создать базу данных
sudo -u postgres createdb nudge
```

### Шаг 3: Настрой переменные окружения

Скопируй файл с примером:

```bash
cp .env.example .env
```

Открой файл `.env` в редакторе и измени:

```bash
# Для локального PostgreSQL (без Docker)
DATABASE_URL=postgresql://твой_юзер:твой_пароль@localhost:5432/nudge

# Telegram Bot Token (получи у @BotFather в Telegram)
TELEGRAM_BOT_TOKEN=твой_токен_бота
```

**Как получить Telegram Bot Token:**
1. Открой Telegram
2. Найди [@BotFather](https://t.me/BotFather)
3. Отправь команду `/newbot`
4. Следуй инструкциям
5. Скопируй полученный токен в `.env`

**Пример DATABASE_URL для разных случаев:**

```bash
# macOS с Homebrew (обычно без пароля)
DATABASE_URL=postgresql://твой_username@localhost:5432/nudge

# Windows/Linux с паролем
DATABASE_URL=postgresql://postgres:твой_пароль@localhost:5432/nudge

# Если PostgreSQL на другом порту
DATABASE_URL=postgresql://postgres:пароль@localhost:5433/nudge
```

### Шаг 4: Выполни миграции базы данных

Это создаст все необходимые таблицы:

```bash
npm run db:migrate
```

Ты должен увидеть сообщения о создании таблиц.

### Шаг 5: Запусти приложение

```bash
npm run dev
```

Приложение запустится на http://localhost:3000

### Шаг 6: Открой в браузере

Перейди на http://localhost:3000

---

## 🎯 Что ты увидишь после запуска

### Главная страница (Dashboard)
- Текущий стрик и уровень
- Сегодняшние напоминания
- Прогресс до следующего уровня
- Достижения

### Страница контактов (/contacts)
- Список всех контактов
- Поиск по имени и username
- Фильтры по категориям (Семья, Друзья, Коллеги, Бизнес)
- Фильтр по статусу отслеживания

### Страница деталей контакта (/contacts/:id)
- Информация о контакте
- Настройки отслеживания
- Частота связи
- Тип коммуникации
- Категория
- История взаимодействий

---

## 🐛 Решение проблем

### Проблема: "Порт 3000 уже занят"

**Решение:**

```bash
# Найти процесс на порту 3000
lsof -i :3000

# Убить процесс (замени PID на номер из предыдущей команды)
kill -9 <PID>

# Или запусти на другом порту
PORT=3001 npm run dev
```

### Проблема: "Cannot connect to database"

**Проверь:**

1. PostgreSQL запущен?
   ```bash
   # macOS
   brew services list | grep postgresql
   
   # Linux
   sudo systemctl status postgresql
   ```

2. Правильный ли DATABASE_URL в `.env`?
   ```bash
   cat .env | grep DATABASE_URL
   ```

3. База данных создана?
   ```bash
   psql -l | grep nudge
   ```

**Решение:**

```bash
# Перезапусти PostgreSQL
brew services restart postgresql@15  # macOS
sudo systemctl restart postgresql    # Linux

# Создай базу заново
createdb nudge

# Выполни миграции
npm run db:migrate
```

### Проблема: "Module not found" или ошибки импорта

**Решение:**

```bash
# Удали node_modules и переустанови
rm -rf node_modules
npm install

# Очисти кэш Nuxt
rm -rf .nuxt .output
npm run dev
```

### Проблема: Миграции не выполняются

**Решение:**

```bash
# Проверь подключение к БД
psql $DATABASE_URL

# Если подключение работает, попробуй:
npm run db:generate
npm run db:migrate
```

### Проблема: Docker контейнеры не запускаются

**Решение:**

```bash
# Остановить всё
make down

# Удалить всё (включая volumes)
make clean

# Запустить заново
make quickstart
```

---

## 📊 Проверка, что всё работает

### 1. Проверь health endpoint

```bash
curl http://localhost:3000/api/health
```

Должен вернуть:
```json
{
  "status": "ok",
  "timestamp": "2025-01-02T...",
  "uptime": 123.45,
  "environment": "development"
}
```

### 2. Проверь базу данных

```bash
# С Docker
make shell-db

# Без Docker
psql $DATABASE_URL

# Внутри psql:
\dt  # Показать все таблицы
```

Должны быть таблицы: `users`, `contacts`, `reminders`, `achievements`, и т.д.

### 3. Проверь API endpoints

```bash
# Получить контакты (замени userId на реальный)
curl "http://localhost:3000/api/contacts?userId=1"

# Получить напоминания
curl "http://localhost:3000/api/reminders?userId=1"

# Получить статистику геймификации
curl "http://localhost:3000/api/gamification/stats?userId=1"
```

---

## 🎨 Режимы запуска

### Development режим (с hot-reload)

```bash
# С Docker
make dev

# Без Docker
npm run dev
```

**Особенности:**
- ✅ Автоматическая перезагрузка при изменении кода
- ✅ Source maps для отладки
- ✅ Подробные ошибки в браузере

### Production режим

```bash
# С Docker
make quickstart

# Без Docker
npm run build
npm run preview
```

**Особенности:**
- ✅ Оптимизированная сборка
- ✅ Минификация кода
- ✅ Быстрая загрузка

---

## 🔧 Полезные команды

### С Docker:

```bash
make help          # Все доступные команды
make status        # Статус контейнеров
make logs          # Логи в реальном времени
make shell         # Shell в app контейнере
make shell-db      # psql консоль
make migrate       # Выполнить миграции
make test          # Запустить тесты
make restart       # Перезапустить
make clean         # Удалить всё
```

### Без Docker:

```bash
npm run dev        # Запустить dev сервер
npm run build      # Собрать для production
npm run preview    # Предпросмотр production сборки
npm test           # Запустить тесты
npm run db:migrate # Выполнить миграции
npm run db:studio  # Открыть Drizzle Studio
```

---

## 📱 Тестирование с Telegram

Чтобы протестировать как Telegram Mini App:

1. **Создай бота** у @BotFather
2. **Получи токен** и добавь в `.env`
3. **Настрой webhook** (для production)
4. **Открой Mini App** через Telegram

Подробнее в документации Telegram: https://core.telegram.org/bots/webapps

---

## ✅ Чеклист успешного запуска

- [ ] Docker установлен (для варианта 1) ИЛИ PostgreSQL установлен (для варианта 2)
- [ ] Node.js 20+ установлен
- [ ] Зависимости установлены (`npm install`)
- [ ] Файл `.env` настроен
- [ ] База данных создана
- [ ] Миграции выполнены
- [ ] Приложение запущено
- [ ] http://localhost:3000 открывается в браузере
- [ ] API endpoints отвечают

---

## 🎉 Готово!

Если всё работает, ты увидишь:
- ✅ Dashboard с виджетами
- ✅ Страницу контактов с фильтрами
- ✅ Страницу деталей контакта с настройками

**Следующие шаги:**
1. Изучи код в `pages/`, `composables/`, `server/api/`
2. Добавь тестовые данные через Adminer
3. Начни разработку новых фич!

**Нужна помощь?**
- Смотри [README.md](./README.md)
- Смотри [README.Docker.md](./README.Docker.md)
- Проверь раздел "Решение проблем" выше

Удачи! 🚀
